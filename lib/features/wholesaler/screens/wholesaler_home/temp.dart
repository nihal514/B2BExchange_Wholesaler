import 'package:flutter/material.dart';

class WholeSalerFetures extends StatelessWidget {
  const WholeSalerFetures({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text("Wholesaler Fetures"),);

  }
}
